# ============================================
# 贈与税申告 必要書類案内 - Production Override
# ============================================
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
# ============================================

x-prod-deploy: &prod-deploy
  resources:
    limits:
      memory: 256M
    reservations:
      memory: 64M

services:
  gift-tax-docs:
    build:
      target: runner
    init: false
    command: ["nginx", "-g", "daemon off;"]
    volumes: []
    deploy: *prod-deploy
