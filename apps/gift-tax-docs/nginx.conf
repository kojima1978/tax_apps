server {
    listen 3002;
    listen [::]:3002;
    server_name localhost;
    root /usr/share/nginx/html;

    # nginxバージョン情報を非表示
    server_tokens off;

    # セキュリティヘッダー
    include /etc/nginx/snippets/security-headers.conf;

    # SPA用ルーティング（Viteの base: '/gift-tax-docs/' に対応）
    location /gift-tax-docs/ {
        try_files $uri $uri/ /gift-tax-docs/index.html;

        # index.htmlはハッシュなしのため、常に再検証させる
        location = /gift-tax-docs/index.html {
            include /etc/nginx/snippets/security-headers.conf;
            add_header Cache-Control "no-cache" always;
        }
    }

    # ルートからのリダイレクト
    location = / {
        return 301 /gift-tax-docs/;
    }

    # Viteビルド成果物のキャッシュ設定（ハッシュ付きファイル名のため長期キャッシュ可能）
    location /gift-tax-docs/assets/ {
        include /etc/nginx/snippets/security-headers.conf;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        access_log off;
    }

    # gzip圧縮
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml
        image/svg+xml;
}
