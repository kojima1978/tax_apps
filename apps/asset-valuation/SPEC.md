# Asset Valuation（相続税 減価償却資産評価）機能仕様書

## 1. 概要

相続税申告における減価償却資産の評価額を算出するWebアプリケーション。
会計ソフトから出力されたCSVファイルを取り込み、相続税法の評価通達に基づいて計算し、Excel出力・印刷を行う。

### 1.1 技術スタック

| 項目 | 技術 |
|------|------|
| フロントエンド | Vite 7.x + React 19.x + TypeScript 5.x |
| スタイリング | Tailwind CSS v4（@tailwindcss/postcss） |
| アイコン | lucide-react |
| Excel出力 | xlsx-js-style |
| テーマカラー | 緑（#2e7d32） |
| Docker ポート | 3017 |
| basePath | `/asset-valuation` |

### 1.2 アーキテクチャ

- **完全フロントエンド**（バックエンド不要）
- 未償却残高表はJSONデータとして内蔵（H24.4.1以後取得分）
- CSV解析はブラウザ内で完結（最大300件）
- データ永続化: JSON入出力（案件データ・マッピングプリセット）
- Docker: Vite dev → nginx:1.27-alpine 静的配信

---

## 2. 画面仕様

### 2.1 画面構成

単一ページ・ステップ形式（4ステップ）

```
Step 1: CSVインポート + 基本情報入力
    ↓
Step 2: カラムマッピング
    ↓
Step 3: データ確認・編集
    ↓
Step 4: 計算結果 → Excel出力 / 案件JSON保存 / マッピングJSON出力
```

ステップ間は「次へ」「戻る」ボタンで遷移。各ステップにバリデーションあり。

---

### 2.2 Step 1: CSVインポート + 基本情報入力

| 項目 | 仕様 |
|------|------|
| 案件名 | テキスト入力（必須）。例：「株式会社〇〇〇 様」 |
| 課税時期 | 日付入力（必須） |
| CSVファイル | ファイルドロップ or ファイル選択 |
| エンコーディング | UTF-8, CP932 (Shift_JIS) 自動検出 |
| 案件JSONインポート | JSONファイルをアップロードして過去案件を復元 |

#### CSV要件
- ヘッダー行あり（1行目）
- 最大300件の資産データ

---

### 2.3 Step 2: カラムマッピング

CSVのカラムを、計算に必要なフィールドへマッピングする画面。

#### 2.3.1 必須マッピングフィールド

| フィールド | 説明 | 型 |
|-----------|------|------|
| 資産カテゴリ | 建物/建物付属設備/構築物/機械装置/車両/器具備品 | テキスト |
| 資産名称 | 減価償却資産の名称等 | テキスト |
| NO | 管理番号 | 数値 |
| 取得年月 | 取得日（日付 or Excelシリアル値） | 日付 |
| 耐用年数 | 法定耐用年数 | 数値 |
| 取得価額 | 取得時の価額 | 数値 |
| 期末簿価 | 直近期末の帳簿価額 | 数値 |

#### 2.3.2 マッピングUI

- CSVの先頭5行をプレビュー表示
- 各フィールドにドロップダウンでCSVカラムを割り当て
- ドロップダウン変更時、プレビューが即座に更新
- 未マッピングの必須フィールドは赤色で警告

#### 2.3.3 マッピングプリセット

| 機能 | 仕様 |
|------|------|
| 保存先 | localStorage + JSON入出力 |
| 保存 | 現在のマッピングに名前を付けて保存 |
| 読込 | 保存済みプリセットをドロップダウンで選択→即適用 |
| 削除 | 不要なプリセットを削除 |
| JSONエクスポート | プリセットをJSONファイルとしてダウンロード |
| JSONインポート | JSONファイルをアップロードしてプリセットを追加 |

#### 2.3.4 カテゴリ名の正規化

CSVの資産カテゴリ値を、アプリ内部の10カテゴリにマッピングする。

| 内部カテゴリ | CSVでの表現例 |
|-------------|-------------|
| 建物 | 建物、たてもの |
| 建物付属設備 | 建物付属設備、建物附属設備、付属設備 |
| 構築物 | 構築物 |
| 機械装置 | 機械装置、機械及び装置 |
| 車両 | 車両、車両及び運搬具、車両運搬具 |
| 器具備品 | 器具備品、器具及び備品、工具器具備品 |
| ソフトウェア | ソフトウェア、ソフトウエア |
| 無形固定資産 | 無形固定資産、無形資産 |
| 繰延資産 | 繰延資産 |
| 一括償却資産 | 一括償却資産、一括償却 |

- 自動マッチング後、未マッチの値はユーザーが手動で割り当て
- カテゴリマッピングもプリセットに含めて保存

---

### 2.4 Step 3: データ確認・編集

#### 2.4.1 テーブル表示

- カテゴリ別にグループ化して表示（建物 → 建物付属設備 → 構築物 → 機械装置 → 車両 → 器具備品）
- 各カテゴリにヘッダー行（カテゴリ名 + 件数）

#### 2.4.2 テーブルカラム

| カラム | 編集可 | 説明 |
|--------|--------|------|
| NO | ○ | 管理番号 |
| 資産名称 | ○ | 名称等 |
| 取得年月 | ○ | 日付表示 |
| 課税時期 | — | Step 1で入力した値（全行共通） |
| 経過年数 | — | 自動計算（リアルタイム） |
| 耐用年数 | ○ | |
| 取得価額 | ○ | 金額フォーマット表示 |
| 償却額/償却率 | — | 自動計算（建物は償却額、他は償却率） |
| 相続税評価額 | — | 自動計算 |
| 期末簿価 | ○ | |
| 評価根拠 | — | 自動判定（評基通89-2(2)、92、97、129等） |
| 固定資産税評価明細 | ○ | チェックボックス（建物・建物付属設備のみ表示） |
| 賃貸 | ○ | チェックボックス（建物・建物付属設備のみ表示）。ONの場合、評価額 × 0.7（借家権割合30%控除） |

#### 2.4.3 操作機能

| 機能 | 仕様 |
|------|------|
| 行の編集 | インライン編集。値変更で即座に再計算 |
| 行の削除 | ダブルクリック確認付き削除（1回目で赤色ハイライト、2回目で実行） |
| 行の追加 | カテゴリを指定して空行追加（手入力用） |
| 並び替え | カテゴリ内でNO順 / 取得年月順 / 金額順 |
| 固定資産税評価明細 一括チェック | 建物・建物付属設備それぞれに全行ON/OFFトグル |
| 3年以内ハイライト | 課税時期から3年以内の取得は背景色でハイライト（自動判定） |

---

### 2.5 Step 4: 計算結果

#### 2.5.1 結果表示

- サマリーカード（取得価額合計、相続税評価額合計）
- カテゴリ別内訳
- Excelプレビューと同じレイアウトで表示
- 計算根拠一覧（カテゴリ別・NO昇順）
- 「新規案件を開始」ボタン

#### 2.5.2 出力機能

| 機能 | 仕様 |
|------|------|
| Excel出力 | sample.xlsm準拠レイアウト。A4横・全列1ページ・横線のみ罫線・フッターページ番号。2シート（減価償却資産＋計算根拠） |
| 案件JSONエクスポート | 案件データ全体（基本情報 + 資産データ + 設定）をJSON出力 |
| マッピングJSON出力 | マッピングプリセットをJSONファイルとしてダウンロード |

---

## 3. 計算仕様

### 3.1 共通計算

#### 3.1.1 経過年数

```
経過年数 = ROUNDUP((課税時期 - 取得年月) / 365, 0)
```

- 1年未満の端数は1年に切り上げ

#### 3.1.2 3年以内判定

```
3年以内 = 取得年月 > (課税時期 - 365 × 3)
```

- 該当カテゴリ: 建物、建物付属設備、構築物

### 3.2 カテゴリ別計算ルール

#### 3.2.1 建物

| 条件 | 評価根拠 | 計算式 |
|------|---------|--------|
| 固定資産税評価明細あり | 評基通89 | 相続税評価額 = `-`（対象外） |
| 3年以内取得 | 3年内簿価 | 相続税評価額 = 期末簿価 |
| 上記以外 | 評基通89-2(2) | 相続税評価額 = `(取得価額 - 償却額) × 0.7` |

**賃貸の場合（建物）:** 上記で算出した相続税評価額に対し、さらに `× 0.7`（借家権割合30%控除）を適用。
固定資産税評価明細の行（評価額 = `-`）には適用しない。

**建物の償却額（定額法）:**

```
経過年数 ≧ 耐用年数の場合:
  償却額 = 取得価額 × 0.9

経過年数 < 耐用年数の場合:
  償却額 = 取得価額 × 0.9 × (経過年数 / 耐用年数)
```

**判定優先順位:** 固定資産税評価明細 > 3年以内 > 89-2(2)

#### 3.2.2 建物付属設備

| 条件 | 評価根拠 | 計算式 |
|------|---------|--------|
| 固定資産税評価明細あり | 評基通89 | 相続税評価額 = `-`（対象外） |
| 3年以内取得 | 3年内簿価 | 相続税評価額 = 期末簿価 |
| 上記以外 | 評基通92 | 相続税評価額 = `ROUNDDOWN(取得価額 × 未償却残額割合 × 0.7, 0)` |

**賃貸の場合（建物付属設備）:** 上記で算出した相続税評価額に対し、さらに `× 0.7`（借家権割合30%控除）を適用。
固定資産税評価明細の行（評価額 = `-`）には適用しない。

#### 3.2.3 構築物

| 条件 | 評価根拠 | 計算式 |
|------|---------|--------|
| 3年以内取得 | 3年内簿価 | 相続税評価額 = 期末簿価 |
| 上記以外 | 評基通97 | 相続税評価額 = `ROUNDDOWN(取得価額 × 未償却残額割合 × 0.7, 0)` |

#### 3.2.4 機械装置

| 評価根拠 | 計算式 |
|---------|--------|
| 評基通129 | 相続税評価額 = `ROUNDDOWN(取得価額 × 未償却残額割合, 0)` |

#### 3.2.5 車両及び運搬具

| 評価根拠 | 計算式 |
|---------|--------|
| 評基通129 | 相続税評価額 = `ROUNDDOWN(取得価額 × 未償却残額割合, 0)` |

#### 3.2.6 器具及び備品

| 評価根拠 | 計算式 |
|---------|--------|
| 評基通129 | 相続税評価額 = `ROUNDDOWN(取得価額 × 未償却残額割合, 0)` |

### 3.3 未償却残額割合

**定率法未償却残額表（平成24年4月1日以後取得分）** を使用。

```
未償却残額割合 = rateTable[経過年数][耐用年数]
```

- 経過年数 ≧ 耐用年数の場合: 未償却残額割合 = 0
- テーブル範囲: 経過年数 1〜50、耐用年数 2〜50

---

## 4. Excel出力仕様

### 4.1 レイアウト

sample.xlsmの「減価償却資産」シートを再現する。

```
行1: 案件名（「株式会社〇〇〇　様」）
行2: 課税時期: YYYY/MM/DD
行3: 3年以内: YYYY/MM/DD
行4: （空行）
行5: 【カテゴリ名】
行6: ヘッダー行（NO, 名称, 取得年月, 課税時期, 経過年数, 耐用年数, 取得価額, 償却額/償却率, 相続税評価額, 期末簿価, その他）
行7〜: データ行
行N: 合計行（取得価額合計, 相続税評価額合計, 期末簿価合計）
（空行）
次のカテゴリ...
（全カテゴリ終了後）
評価通達条文テキスト
```

### 4.2 カラム仕様

| カラム | 建物 | 建物付属設備/構築物/機械装置/車両/器具備品 |
|--------|------|----------------------------------------|
| H列 | 償却額（金額） | 償却率（小数） |

### 4.3 書式

| 要素 | 書式 |
|------|------|
| フォント | MS ゴシック, サイズ10 |
| ヘッダー行 | 背景色(#D9E1F2)付き、太字、上下横線のみ（縦線なし） |
| データ行 | 罫線なし |
| 金額 | 3桁カンマ区切り、右寄せ |
| 日付 | YYYY/MM/DD |
| 合計行 | 太字、A列から全列に上横線 |
| 評価額 `-` | 中央寄せ |
| 印刷設定 | A4横、全列を1ページ幅に収める、余白狭め |
| フッター | ページ番号（ページ / 総ページ） |
| ファイル名 | `{案件名}_減価償却資産評価_{YYYYMMDD}.xlsx` |

### 4.4 評価通達条文

Excel末尾に以下の条文テキストを記載:

- 評基通89-2（文化財建造物である家屋の評価）
- 評基通92（附属設備等の評価）
- 評基通97（構築物の評価）
- 評基通128〜130（一般動産の評価・償却費の額の計算）

---

## 5. JSON仕様

### 5.1 案件データJSON

```json
{
  "version": "1.0",
  "exportedAt": "2026-03-22T10:00:00.000Z",
  "caseName": "株式会社〇〇〇 様",
  "taxDate": "2025-08-01",
  "assets": [
    {
      "no": 9,
      "category": "建物",
      "name": "本社ビル",
      "acquisitionDate": "2009-02-15",
      "usefulLife": 31,
      "acquisitionCost": 57453529,
      "bookValue": 26328088,
      "hasFixedAssetTaxRecord": true,
      "isRental": false,
      "evaluationAmount": null,
      "evaluationBasis": "固定資産税評価明細"
    }
  ]
}
```

### 5.2 マッピングプリセットJSON

```json
{
  "version": "1.0",
  "presets": [
    {
      "name": "弥生会計",
      "columnMapping": {
        "category": "勘定科目",
        "name": "資産名",
        "no": "管理番号",
        "acquisitionDate": "取得年月日",
        "usefulLife": "耐用年数",
        "acquisitionCost": "取得価額",
        "bookValue": "期末帳簿価額"
      },
      "categoryMapping": {
        "建物": "建物",
        "建物附属設備": "建物付属設備",
        "構築物": "構築物",
        "機械及び装置": "機械装置",
        "車両及び運搬具": "車両",
        "工具器具備品": "器具備品"
      }
    }
  ]
}
```

---

## 6. データ仕様

### 6.1 未償却残高表

**定率法未償却残額表（平成24年4月1日以後取得分）** をJSONとしてアプリに内蔵。

- 行: 経過年数（1〜50）
- 列: 耐用年数（2〜50）
- 値: 未償却残額割合（小数、0〜1）
- データソース: sample.xlsmの「未償却残高表」シート

### 6.2 資産カテゴリ

| カテゴリ | 償却方法 | ×0.7 | 固定資産税評価明細 | 3年以内判定 | 賃貸控除 | 評価根拠 |
|---------|---------|------|-----------------|-----------|---------|---------|
| 建物 | 定額法 | あり | あり | あり | あり | 評基通89-2(2) |
| 建物付属設備 | 定率法 | あり | あり | あり | あり | 評基通92 |
| 構築物 | 定率法 | あり | なし | あり | なし | 評基通97 |
| 機械装置 | 定率法 | なし | なし | なし | なし | 評基通129 |
| 車両 | 定率法 | なし | なし | なし | なし | 評基通129 |
| 器具備品 | 定率法 | なし | なし | なし | なし | 評基通129 |
| ソフトウェア | — | — | なし | なし | なし | 評基通129 |
| 無形固定資産 | — | — | なし | なし | なし | 財産性なし |
| 繰延資産 | — | — | なし | なし | なし | 財産性なし |
| 一括償却資産 | — | — | なし | なし | なし | 簿価 |

---

## 7. バリデーション

| チェック項目 | タイミング | 種別 |
|------------|----------|------|
| 案件名が空 | Step 1 | エラー |
| 課税時期が未入力 | Step 1 | エラー |
| 必須カラムが未マッピング | Step 2 | エラー |
| 耐用年数が範囲外（2〜50） | Step 3 | エラー |
| 取得年月が課税時期より未来 | Step 3 | 警告 |
| 取得価額が0以下 | Step 3 | エラー |
| 経過年数が耐用年数を大幅超過（2倍以上）かつ簿価が大きい | Step 3 | 警告 |

---

## 8. ファイル構成

```
asset-valuation/
├── index.html
├── vite.config.ts
├── tsconfig.json
├── tsconfig.app.json
├── tsconfig.node.json
├── postcss.config.mjs
├── package.json
├── SPEC.md
├── docker/
│   ├── Dockerfile.vite-static
│   └── nginx.conf
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── index.css
│   ├── types/
│   │   └── index.ts                  # 型定義
│   ├── data/
│   │   └── rateTable.ts              # 未償却残高表JSON
│   ├── utils/
│   │   ├── csvParser.ts              # CSV解析・エンコーディング検出
│   │   ├── calculation.ts            # 評価額計算ロジック（getDepRate, CalcResult型）
│   │   ├── excelExport.ts            # Excel出力（2シート・共通印刷設定定数）
│   │   ├── fileDownload.ts           # JSONダウンロード + exportCaseJson
│   │   ├── formatters.ts             # formatYen, formatDate, formatDepreciation, calcGroupTotals
│   │   └── validators.ts             # バリデーション
│   ├── hooks/
│   │   ├── useAssetData.ts           # 資産データ管理（groupByLabel再利用）
│   │   └── usePresets.ts             # マッピングプリセット管理
│   └── components/
│       ├── StepIndicator.tsx          # ステップインジケーター
│       ├── StepNavigation.tsx         # 共通ナビゲーション（戻る/次へ/Step1に戻る）
│       ├── step1/
│       │   └── CsvImportStep.tsx      # CSVインポート + 基本情報
│       ├── step2/
│       │   ├── ColumnMappingStep.tsx  # カラムマッピング
│       │   └── PresetManager.tsx      # プリセット管理UI
│       ├── step3/
│       │   ├── DataEditStep.tsx       # データ確認・編集
│       │   ├── AssetTable.tsx         # 資産テーブル
│       │   └── ExcelPreview.tsx       # Excelプレビュー（Step 4で使用）
│       └── step4/
│           └── ResultStep.tsx         # 計算結果・出力
└── docker-compose.yml                 # スタンドアロン起動用
```

---

## 9. Docker仕様

### 9.1 開発モード

```dockerfile
FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
EXPOSE 3017
CMD ["npx", "vite", "--host", "--port", "3017"]
```

### 9.2 本番モード

```dockerfile
FROM node:22-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:1.27-alpine AS runner
COPY --from=builder /app/dist /usr/share/nginx/html/asset-valuation
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 3017
```

### 9.3 docker-compose.yml

```yaml
volumes:
  - ./src:/app/src:ro
  - ./index.html:/app/index.html:ro
```

ホットリロード対応。

---

## 10. UI/UX仕様

### 10.1 アクセシビリティ

| 項目 | 対応 |
|------|------|
| フォーカスリング | 全インタラクティブ要素に統一（`outline: 2px solid #16a34a`） |
| aria-label | テーブル内全input・checkbox・削除ボタンに設定 |
| コントラスト | テキスト4.5:1以上（gray-500以上を使用） |
| reduced-motion | `prefers-reduced-motion: reduce` でアニメーション無効化 |
| cursor-pointer | 全クリッカブル要素に設定 |

### 10.2 インタラクション

| 項目 | 仕様 |
|------|------|
| ボタン遷移 | 全ボタンに `transition-colors`（150-300ms） |
| 削除確認 | ダブルクリック方式（1回目→赤色ハイライト、2回目→実行、blur→リセット） |
| CSV読込 | Loader2スピナー表示 |
| Excel出力 | ボタン無効化 + Loader2スピナー |
| ナビゲーション | ChevronLeft/Right アイコン付き |
| Step 4完了 | 「新規案件を開始」CTA（FilePlus2アイコン） |

### 10.3 共通コンポーネント

| コンポーネント | 用途 |
|--------------|------|
| StepNavigation | Step 2/3/4共通のナビゲーション（戻る/次へ/Step1に戻る） |
| StepIndicator | 4ステップの進行状況表示 |

---

## 11. リファクタリング方針

| パターン | 適用箇所 |
|---------|---------|
| ユーティリティDRY | `formatDepreciation`（3箇所→1関数）、`calcGroupTotals`（3箇所→1関数） |
| 関数再利用 | `groupByLabel`（types/index.ts）をuseAssetData内で再利用 |
| 型エイリアス | `CalcResult`、`AssetInput`（calculation.ts内の冗長なOmit/Pick削減） |
| ヘルパー抽出 | `getDepRate`（calculateAsset内の重複償却率計算） |
| コンポーネント抽出 | `StepNavigation`（3ステップの同一ナビゲーション） |
| フック統合 | `useJsonExport`廃止 → `exportCaseJson`をfileDownload.tsに統合 |
| 定数化 | `PAGE_SETUP`、`MARGINS`（Excel印刷設定の2箇所重複） |
