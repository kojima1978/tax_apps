services:
  app:
    build: .
    ports:
      - "127.0.0.1:5180:8000"
    volumes:
      - ./public:/app:ro
    read_only: true
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    restart: unless-stopped
