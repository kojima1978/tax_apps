# ============================================
# 所得税・贈与税書類リスト - Production Override
# ============================================
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
# ============================================

x-prod-deploy: &prod-deploy
  resources:
    limits:
      memory: 64M
    reservations:
      memory: 16M

services:
  tax-docs:
    build:
      target: runner
    init: false
    volumes: []
    deploy: *prod-deploy
