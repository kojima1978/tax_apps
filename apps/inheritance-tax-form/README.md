# Inheritance Tax Form（相続税の申告書）

相続税の申告書の各様式を画面上で作成・印刷するWebアプリケーション。
国税庁様式の罫線をそのまま再現し、様式1つ＝A4縦1枚に収まる形で印刷できる。

## 実装対象の帳票（全24）

第1表 / 第1表（続）/ 第2表 / 第4表 / 第4表の2 / 第5表 / 第6表 / 第7表 / 第8の8表 /
第9表 / 第10表 / 第11表 / 第11表の付表1 / 第11表の付表2 / 第11表の付表3 / 第11表の付表4 /
第11の2表 / 第11・11の2表の付表1 / 同（続）/ 同（別表1）/ 第13表 / 第14表 / 第15表 / 第15表（続）

**実装済み（10）**: 第1表・第1表（続）・第2表・第11表・第11表の付表1〜4・第11の2表・第13表
**未実装（14）**: 第4表・第4表の2・第5表・第6表・第7表・第8の8表・第9表・第10表・
第11・11の2表の付表1（＋続・別表1）・第14表・第15表（＋続）

> 「第11表の付表1〜4」（財産の明細書）と「第11・11の2表の付表1」（小規模宅地等の特例）は
> 名前が似ているだけの別の様式。


現在はフロントエンドのみ（バックエンド・DBなし）。入力内容はブラウザの localStorage に保存する。

## 技術スタック

| 項目 | 技術 |
|------|------|
| フロントエンド | Vite 6.x + React 19.x + TypeScript 5.9 |
| スタイリング | 素の CSS（`src/main.css`）。UIライブラリなし |
| 状態管理 | `useFormData` フック + localStorage |
| Docker | `docker/Dockerfile.vite-static` 共有（dev: node:22-alpine / prod: nginx:1.27-alpine） |
| ポート | 3021 |
| basePath | `/inheritance-tax-form` |

依存は `react` / `react-dom` のみ。

## 起動方法

```bash
docker compose up -d --build
```

本番モード:

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

→ http://localhost/inheritance-tax-form/ （直接は http://localhost:3021/inheritance-tax-form/ ）

## 画面構成

```
ツールバー（印刷されない）
  財産を取得した人 −/＋ ・ JSON保存 ・ JSON読込 ・ 印刷 ・ クリア
    ↓
サイドバー（様式一覧）   本文（選択中の様式）
  ☑ 第1表          …  第1表        … 被相続人＋「各人の合計」列＋財産を取得した人1人目
  ☑ 第1表（続）    …  第1表（続）  … 2人目以降を2人ずつ（最大21人＝続10枚）
  ☑ 第2表          …  第2表        … 相続税の総額の計算書（法定相続人6人まで）
  ☑ 第11表         …  第11表       … 相続税がかかる財産の合計表（10人ごとに1枚）
  ☑ 第11の2表      …  第11の2表    … 相続時精算課税適用財産の明細書（贈与を受けた人ごとに1枚）
  ☑ 第11表の付表1  …  付表1        … 財産の明細書（土地・家屋等用。財産8件ごとに1枚）
  ☑ 第11表の付表2  …  付表2        … 財産の明細書（有価証券用）
  ☑ 第11表の付表3  …  付表3        … 財産の明細書（現金・預貯金等用）
  ☑ 第11表の付表4  …  付表4        … 財産の明細書（事業用・家庭用・その他）
  ☑ 第13表         …  第13表       … 債務及び葬式費用の明細書（債務4件・葬式費用5件ごとに1枚）
```

- サイドバーの様式名をクリックすると本文がその様式に切り替わる
- チェックした様式が提出（印刷）対象。**印刷はチェックした様式だけを登録簿の順に連続出力する**
- 第1表は必須、第1表（続）は財産を取得した人が2人以上のときに自動で付くのでチェックを操作できない
- 付表は「人」ではなく「財産」の一覧なので枚数を人数から決められない。用紙の下の `−／＋` で増減する
- 第11の2表は**贈与を受けた人1人につき1枚**を自動で用意する。1枚に年分6行しか無いので、
  精算課税の贈与が7年分以上ある人は用紙の下の `−／＋` で同じ人の枚数を増やせる。
  **記入が1つも無い人の分は印刷しない**（画面には出す）
- 第13表は債務4件・葬式費用5件で1枚。件数が多いときは用紙の下の `−／＋` で増やす。
  3の「債務などを承継した人」が1枚に4人分しか入らないので、**財産を取得した人が5人以上いると
  枚数は自動で増える**（`−` はその下限までしか減らせない）
- 様式の登録簿は `App.tsx` の `FORMS` 配列。様式を足すときはここに1行追加する
- 付表4種は作りが同じなので、画面も `App.tsx` の `DETAIL_SPECS` から組み立てている
  （付表を足すときは `FORMS` と `DETAIL_SPECS` に1行ずつ）

## 様式の再現方式（GridForm）

背景画像を敷かず、**罫線そのものを CSS グリッドで引く**。stock-valuation-form と同じ方式。

- セルは `{ top, left, width, height }` を**パーセント**で宣言する（`src/forms/*.ts`）
- `snapLines()` が誤差 0.3% 以内の罫線をまとめ、`grid-template-columns/rows` を `fr` で生成する
- 文字サイズは `fitFontSize()` が枠の実寸から算出して自動で縮める。
  様式は同じ幅の枠に文字数の違うラベルを詰め込むため、文字数だけで決めると
  「算出税額（第３表⑬）」のような長いラベルが枠からはみ出す
- 縦書きの帯（「この申告書が修正申告書である場合」等）は `writing-mode` で表現する

セルの生成は `src/forms/geometry.ts` にまとめてある。人ごとの列（`personColumn`）、
被相続人の列（`decedentColumn`）、①〜㉗の行（`calcRows`）はすべてここのファクトリが返すので、
第1表と第1表（続）で同じ定義を共有できる。

付表1〜4は「8明細×3行＋項番＋分割が確定した財産」という同じ骨格を持つので、
組み立ては `detail.ts` に1本化してある。ただし**罫線の実測pxは様式ごとに違う**
（4様式は別々に版が起こされていて、表の幅は付表1が 1037px・付表2が 1105px…と一致しない）。
そのため座標は共有せず、様式ごとの `DetailFrame` として `table11f?.ts` が持つ。
付表3・付表4は組の中の横罫線が3行のグリッドと揃わないため、`rowLines` と `bands` で
「どの罫線からどの罫線まで」を明示する。

## フィールド名の規約

`GridForm` は `g(field)` / `u(field, value)` の2関数だけで値をやり取りする。
保存先は接頭辞で振り分ける（`useFormData` の `splitField`）。

| 接頭辞 | 保存先 |
|--------|--------|
| `c.` | 共通欄（被相続人・提出先・第2表㋭） |
| `t.` | 自動計算欄（「各人の合計」列と様式間の転記欄・書き込み不可） |
| `h0.` `h1.` … | 財産を取得した人 N番目 |
| `l0.` `l1.` … | 第2表の法定相続人 N番目 |
| `table11f1#0.` `table11f2#0.` … | 付表（財産の明細書）の様式ID＋明細の通し番号 |

## 自動計算

自動計算は `src/lib/calc.ts` に閉じている。

### 第1表・第1表（続）

| 欄 | 算式 |
|----|------|
| ④ 純資産価額 | ①＋②−③（赤字は0） |
| ⑥ 課税価格 | ④＋⑤（1,000円未満切捨て） |
| ⑧ あん分割合 | 各人の⑥ ÷ Ⓐ（小数2位）。**端数調整のため手入力で上書き可**（空に戻すと自動へ復帰） |
| ⑨ 算出税額 | ⑦ × ⑧（円未満切捨て） |
| ⑮ 計 | ⑫＋⑬＋⑭ |
| ⑯ 差引税額 | ⑨＋⑪−⑮（⑩が記入されていれば⑩＋⑪−⑮。赤字は0） |
| ⑲ 小計 | ⑯−⑰−⑱（黒字は100円未満切捨て） |
| ㉑ / ㉒ | ⑲−⑳ が黒字なら㉑、赤字なら㉒（還付） |
| ㉖ 小計の増加額 | ⑲−㉓ |
| ㉗ 納付／還付 | （㉑又は㉒）−㉕（還付は△。黒字は100円未満切捨て） |
| 「各人の合計」列 | 各行の横計。Ⓐは⑥の合計、⑧の合計欄は様式どおり `1.00` 固定 |

### 第2表（相続税の総額の計算書）

| 欄 | 算式 |
|----|------|
| ㋑ 課税価格の合計額 | 第1表Ⓐ（＝⑥の合計）を自動転記 |
| ㋺ 法定相続人の数 | ④欄に氏名を書いた人数 |
| ㋩ 遺産に係る基礎控除額 | 3,000万円＋600万円×㋺（万円単位） |
| ㋥ / ㋬ 課税遺産総額 | ㋑−㋩ ／ ㋭−㋩（基礎控除以下なら0） |
| ⑥ / ⑨ 法定相続分に応ずる取得金額 | ㋥×⑤ ／ ㋬×⑤（1,000円未満切捨て） |
| ⑦ / ⑩ 税額 | 速算表（`RATE_BRACKETS`）で ⑥（⑨）×税率−控除額 |
| ⑧ / ⑪ 相続税の総額 | ⑦（⑩）の合計（100円未満切捨て） |

㋭「第3表⑥Ⓐ」だけが手入力（第3表は未対応）。⑤法定相続分は分子・分母を入力する。

### 第11の2表（相続時精算課税適用財産の明細書）

| 欄 | 算式 |
|----|------|
| 1⑤ 相続時精算課税適用財産の価額 | ③−④（赤字は0） |
| 1⑧ / 1⑨ / 1⑩ | その人の**全枚数を通した** ⑤ ／ ⑥ ／ ⑦ の合計。**合計欄は最終ページにだけ出す** |

枚数は人ごとに `t112Pages`（その人の欄）で持つ。枚数を減らしても入力値は消さず、
**表に出ている行だけを集計する**ので、枚数を戻せば記入内容が戻る。

### 第13表（債務及び葬式費用の明細書）

| 欄 | 算式 |
|----|------|
| 1 合計 / 2 合計 | 全枚数を通した債務の金額 ／ 葬式費用の金額の合計 |
| 3① 負担することが確定した債務 | 1の各行の「負担する金額」を、選んだ「負担する人」ごとに合計 |
| 3④ 負担することが確定した葬式費用 | 2の各行の「負担する金額」を同様に合計 |
| 3③ 計 / 3⑥ 計 / 3⑦ 合計 | ①＋② ／ ④＋⑤ ／ ③＋⑥ |
| （各人の合計）列 | ①〜⑦の各行を人数分横に合計 |

- 1・2の明細は様式全体で1つの一覧なので、付表と違い**共通欄（`c.`）に `t13d0…` / `t13f0…`
  として持つ**。枚数は `c.t13Pages`
- 「負担する人の氏名」は**選択式**。値は財産を取得した人の項番（1始まり）で、印字は氏名になる
  （名前を直したときに書き換え漏れが起きない）
- 3の人の欄は1枚に4人分。**枚数を減らしても入力値は消さず、表に出ている行だけを集計する**
- ②「負担することが確定していない債務」と⑤は手入力（未確定分は明細に書かない様式のため）

### 様式間の転記

**転記先は読み取り専用**にして、書き写しのずれが起きないようにしている。

| 転記先（第1表） | 転記元（第2表） |
|------|------|
| Ⓑ 遺産に係る基礎控除額 | ②の㋩（百万円単位に換算） |
| ⑦ 相続税の総額 | ⑧ |
| 法定相続人の数 | ②の㋺ |

| 転記先（第1表） | 転記元（第11表） |
|------|------|
| ① 取得財産の価額 | 2③（＝①分割財産＋②未分割財産） |

| 転記先（第1表） | 転記元（第11の2表） |
|------|------|
| ② 相続時精算課税適用財産の価額 | 1⑧（その人の全枚数を通した⑤の合計） |
| ⑰ 相続時精算課税分の贈与税額控除額 | 1⑨（同⑥の合計。⑰は百円単位の欄なので100で割る） |

| 転記先（第1表） | 転記元（第13表） |
|------|------|
| ③ 債務及び葬式費用の金額 | 3⑦（その人の合計） |

その様式のチェックを外すと転記先は手入力欄に戻る（`App.tsx` の `TRANSFERRED_BY_FORM`）。

| 転記先（第11表） | 転記元（付表1〜4） |
|------|------|
| 2① 分割財産の価額 | 「分割が確定した財産」の取得財産の価額を「財産を取得した人の番号」ごとに合計 |

付表を1件でも入力すると第11表2①は読み取り専用になる。②未分割財産は付表に人ごとの欄が
無いため手入力のまま（付表の明細は「分割が確定した財産」だけを人に割り当てる様式）。

`computeAll()` は ⑥→Ⓐ→（第2表）⑧→⑨ の順に第1表を2周する。循環はしない。

**単位の扱い**: 様式に「000」「00」があらかじめ印字されている欄（⑥・Ⓑ・⑦・⑰・⑳・㉑・㉔）は
上位桁だけを状態に持ち、計算時のみ `SCALE` 倍して円に戻す。表示・保存が様式の記載と一致する。

**空欄の扱い**: 算式に使う欄が1つも埋まっていない行は 0 ではなく空欄のままにする（白紙の様式を0で埋めない）。

## 入力補助

- 生年月日の元号（1:明治〜5:令和）と続柄（01:配偶者、11:長男 …）は記載要領等のコードから選択式（`src/data/codes.ts`）
- 付表1〜4のコード欄（細目コード／口座種別等コード、特例、金融商品取引業者等・金融機関等・支店等コード、国外）も
  記載要領のコード表から選択式（`src/data/detailCodes.ts`）。**細目コードを選ぶと隣の「細目」「口座種別等」に
  名称が自動で入る**が、読み取り専用にはしない（付表3の「99 上記以外」・付表4の「73 その他」は自分で書くため）
- コード欄は選択後、様式には**コードの数字だけ**が出る（`compactSelectedOption`）。意味は選択肢を開けば読める
- 記載要領由来で間違えやすい点を2つコード表側に閉じ込めてある。
  **金融機関のコードは付表2と付表3で違う**（付表2は 6:証券・7:上記以外、付表3は 5:漁協・6:上記以外）。
  **特例の番号も様式ごとに使える範囲が違う**（付表1は1〜6、付表2は3・6〜8、付表4は2・4・6、付表3に欄は無い）
- 細目コードには第15表の転記先（`table15`）も持たせてある。第15表は未実装だが、
  細目ごとの合計をどの欄へ持っていくかは記載要領で決まっているのでコード表を唯一の定義元にする。
  **利用区分によっては転記先が1欄増える**（付表1の記載要領 注1・注2）ので、その例外も
  `table15Extra` としてコード表に持たせた。宅地の「配偶者居住権に基づく敷地利用権」は③に加えて⑦へ、
  家屋等の「配偶者居住権」は⑩に加えて⑪へ。判定は**完全一致**で行うこと —
  「配偶者居住権の目的となっている建物」は文字列としては「配偶者居住権」を含むが⑪の対象ではない
- 様式にも記載要領にも書かれておらず**記載例にしか出てこない記入条件**は、入力欄のツールチップ
  （`GridCell` の `hint`）として添える。下の「記載例から取り込んだ記入ルール」を参照
- 「※」の付いた欄（※通信日付印の年月日・※確認・※申告期限延長日）は記入不要のため入力欄を置いていない
- 入力は変更のたびに localStorage（`inheritance-tax-form:v1`）へ保存される
- JSON保存／読込でファイルとしても持ち出せる

## 記載例から取り込んだ記入ルール

「相続税の申告書の記載例」（国税庁）にしか書かれていない記入条件がいくつかある。
様式・記載要領だけを見て作ると気づけないので、該当欄の `hint`（ツールチップ）として残してある。

| 欄 | 条件 | 出典 |
|----|------|------|
| 付表1 持分割合 | 被相続人が有していた持分割合を記入。**単独所有の財産は記入不要** | 54ページ |
| 付表1 備考 | 区分所有財産は敷地利用権（敷地権）の割合、家族名義の財産はその名義 | 54ページ |
| 付表2 国外 | 所在が国外でも、**国内の金融商品取引業者等の口座で管理されていたものは記入不要** | 56ページ |
| 付表3 国外 | 判定するのは財産の所在ではなく「**預入れをしていた営業所**」の所在 | 58ページ |
| 付表3 備考 | 家族名義の財産はその名義を記入 | 58ページ |
| 付表4 倍数 | 相続人・包括受遺者が取得した立木は時価の85％で評価するため「0.85」 | 60ページ |
| 付表4 価額 | **代償財産（細目コード75）は価額を「0」**とし、授受する額は「取得財産の価額」欄に書く | 62ページ |
| 第11の2表 1④ | 被相続人（特定贈与者）が死亡した年分は110万円。同一年に2人以上の特定贈与者から贈与を受けていれば贈与税の課税価格であん分する | 63ページ |
| 第11の2表 1⑥ | 贈与税額に**利子税・延滞税・加算税は含めない** | 63ページ |
| 第11の2表 1⑦ | 2に記載した財産について**贈与税の外国税額控除の適用を受けている場合**のみ記入 | 63ページ |

### 代償財産と負数

代償財産は**支払う人が負数・受け取る人が正数**で「取得財産の価額」欄に書く（62ページ）。
そのため付表の同欄と、その転記先である第11表2①③・第1表①は `signedCommaInteger`（△を許す欄）にしてある。
`calc.ts` の `num()` は `△` を負数として読むので、集計もそのまま通る。

付表4に「75 代償財産・価額0・取得者1に△5,000,000・取得者2に5,000,000」と入れると、
第11表2の1人目①③＝△5,000,000／2人目①③＝5,000,000、第1表①も同じ値になることを確認済み。

## 印刷

`window.print()` を呼ぶだけ。`@media print` でツールバー・サイドバーを落とし、
1ページ＝A4縦1枚（209.9 × 297.0mm）に収まることを確認済み。
画面で選択中でない様式も、チェックが付いていれば印刷される（`.form-pages--hidden` を印刷時だけ戻す）。

## ファイル構成

```
src/
├── App.tsx                     # ページ構成・ツールバー・第1表（続）の枚数制御
├── main.css                    # 様式・印刷レイアウト
├── components/ui/
│   ├── GridForm.tsx            # セル定義 → CSSグリッド。文字サイズ自動調整
│   └── printContext.ts
├── forms/
│   ├── geometry.ts             # セル生成のファクトリ（列・行・帯）
│   ├── table1.ts               # 第1表のセル定義
│   ├── table1cont.ts           # 第1表（続）のセル定義
│   ├── table2.ts               # 第2表のセル定義・速算表
│   ├── table11.ts              # 第11表のセル定義
│   ├── table112.ts             # 第11の2表のセル定義（贈与を受けた人ごと・複数枚対応）
│   ├── detail.ts               # 付表1〜4に共通の組み立て（8明細×3行・項番・分割が確定した財産）
│   ├── table11f1.ts            # 付表1（土地・家屋等用）の罫線と明細の割付
│   ├── table11f2.ts            # 付表2（有価証券用）
│   ├── table11f3.ts            # 付表3（現金・預貯金等用）
│   ├── table11f4.ts            # 付表4（事業用・家庭用・その他）
│   └── table13.ts              # 第13表のセル定義（債務4件・葬式費用5件・承継した人4人／枚）
├── hooks/useFormData.ts        # 入力状態・localStorage・JSON入出力
├── lib/calc.ts                 # 第1表①〜㉗・第2表・第11表・第11の2表・第13表・付表からの集計
└── data/
    ├── codes.ts                # 元号コード・続柄コード
    └── detailCodes.ts          # 付表1〜4のコード表（細目・特例・金融機関等・支店等・国外）
```

## 対象外

- 未実装の計算書・明細書（第3表〜第15表・各付表）— 使用頻度順に順次追加していく
- 第2表の付表（法定相続人が7人以上の場合）
- 電子申告（e-Tax）用データの出力
- サーバー保存（バックエンドなし）
